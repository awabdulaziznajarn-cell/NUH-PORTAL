namespace NUH_PORTAL.Models.Enums
{
    // حالة الوحدة نفسها — مش حالة الساكن.
    // ⚠️ «خارج الخدمة» و«غير موجودة» بيتسجّلوا كصفوف وما بيتشالوش: الصف اللي
    //    مكتوب عليه «غير موجودة» إجابة، والصف الناقص سؤال. لو شِلنا فيلا ٣٧
    //    من القائمة، بعد سنة محدش هيعرف هي اتنسيت ولا مقصودة.
    public enum FacultyUnitStatus
    {
        Active = 1,        // في الخدمة — قابلة للتسكين
        OutOfService = 2,  // موجودة بس مافيش خدمة (برج مش متوصّل)
        NotExists = 3      // الرقم ده مافيش وحدة بيه أصلًا
    }
}
