namespace NUH_PORTAL.Models.Enums
{
    // الحالة الدراسية/السكنية للطالب — بتتخزّن كنص زي ما هي (بدون تغيير عمود).
    // left_housing قيمة موجودة فعليًا (بتتسجّل من StudentStatusService لما الطالب يترك الإسكان)،
    // وإن كانت لسه مالهاش مفتاح ترجمة stu_status_* — بنحافظ على السلوك زي ما هو.
    public enum StudentStatus
    {
        active,
        dismissed,
        graduated,
        transferred,
        left_housing,

        // ⚠️ حالة عامة للحالات اللي مالهاش بند مخصّص. المشرف بيكتب التفاصيل
        //    في «سبب الإجراء» وهو إجباري (٥ أحرف على الأقل) ويقدر يرفق مستند.
        //    مش حالة نهائية عن قصد: «أخرى» معناها غير محدّد، ولو خلّيناها
        //    نهائية هتقفل الطالب ومحدش يقدر يسجّل حالته الحقيقية بعد كده
        //    إلا بصلاحية «تصحيح حالة نهائية». وماتفضّيش السكن لنفس السبب:
        //    مانعرفش لو ساب السكن ولا لأ.
        other
    }
}
