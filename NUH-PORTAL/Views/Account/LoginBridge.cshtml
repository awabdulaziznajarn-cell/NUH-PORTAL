@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head><meta charset="utf-8" /><title>جاري الدخول…</title></head>
<body style="font-family:sans-serif;display:flex;align-items:center;justify-content:center;min-height:100vh;color:#1B2A5E">
  <div>جاري تسجيل الدخول…</div>
  <script type="application/json" id="bridge-data">@Html.Raw(ViewData["BridgeJson"])</script>
  <script>
    (function () {
      var data = JSON.parse(document.getElementById('bridge-data').textContent);
      // نفس مفاتيح الدخول القديم — عشان الصفحات الحالية تشتغل بنفس الجلسة
      localStorage.setItem('staffToken', data.token);
      localStorage.setItem('staffUser', JSON.stringify(data.user));
      location.replace(data.redirect);
    })();
  </script>
</body>
</html>
