@{
    ViewData["Title"] = "تحديث حالة الطالب (المشرف)";
}

<style>
  /* صفحة المشرف — نفس عناصر صفحة حالة الطالب */
  .form-card{background:var(--white);border-radius:16px;border:1px solid var(--gray-100);box-shadow:0 2px 8px rgba(27,42,94,.05);overflow:hidden;margin-bottom:20px}
  .form-card-header{padding:18px 24px;border-bottom:1px solid var(--gray-100);display:flex;align-items:center;gap:10px}
  .form-card-header-icon{width:36px;height:36px;border-radius:10px;background:var(--gray-100);display:flex;align-items:center;justify-content:center}
  .form-card-title{font-size:15px;font-weight:700;color:var(--navy-dark)}
  .form-card-body{padding:24px}
  .form-grid{display:grid;grid-template-columns:1fr 1fr;gap:20px}
  .field{display:flex;flex-direction:column;gap:7px}
  .field.full{grid-column:1/-1}
  .field label{font-size:13px;font-weight:600;color:var(--gray-700)}
  .input-wrap{position:relative}
  .input-wrap input,.input-wrap select{width:100%;height:46px;background:var(--gray-50);border:1.5px solid var(--gray-200);border-radius:10px;padding:0 14px;font-family:'IBM Plex Sans Arabic',sans-serif;font-size:14px;color:var(--navy-dark);transition:all .2s;appearance:none;outline:none}
  .input-wrap input:focus,.input-wrap select:focus{border-color:var(--navy);background:var(--white);box-shadow:0 0 0 3px rgba(27,42,94,.08)}
  .input-wrap input.error,textarea.error{border-color:#EF4444}
  .field-error{font-size:11px;color:#EF4444;font-weight:500;display:none}
  .field-error.show{display:block}
  .status-option{display:flex;align-items:center;gap:12px;padding:12px 16px;border:1.5px solid var(--gray-200);border-radius:10px;cursor:pointer;transition:all .2s;margin-bottom:8px}
  .status-option:hover{border-color:var(--navy-light);background:var(--gray-50)}
  .status-option.selected{border-color:var(--navy);background:#E6F1FB}
  .status-option input[type=radio]{display:none}
  .status-option-text{font-size:14px;font-weight:600;color:var(--navy-dark)}
  .status-option-desc{font-size:12px;color:var(--gray-500)}
  .file-upload-wrap{position:relative}
  .file-upload-wrap input[type=file]{position:absolute;inset:0;opacity:0;cursor:pointer}
  .file-upload-btn{display:inline-flex;align-items:center;gap:8px;padding:10px 20px;background:var(--gray-100);border:1.5px dashed var(--gray-300);border-radius:10px;font-size:13px;color:var(--gray-700);font-weight:500;cursor:pointer;transition:all .2s}
  .file-name{font-size:12px;color:var(--gray-500);margin-top:4px}
  .history-list{max-height:400px;overflow-y:auto}
  .history-item{padding:12px;border-bottom:1px solid var(--gray-100);font-size:13px}
  .badge-status-graduated{background:#DBEAFE;color:#1E40AF}
  .badge-status-dismissed{background:var(--red-light);color:var(--red)}
  .badge-status-transferred{background:#FFF7ED;color:#92400E}
  .badge-status-left_housing{background:#F3E8FF;color:#6B21A8}
  .two-col{display:grid;grid-template-columns:1fr 1fr;gap:20px;margin-bottom:20px}
  .modal-overlay{position:fixed;inset:0;background:rgba(0,0,0,.5);z-index:10000;display:none;align-items:center;justify-content:center;backdrop-filter:blur(2px)}
  .modal-overlay.show{display:flex}
  .modal-box{background:var(--white);border-radius:16px;padding:28px;max-width:480px;width:90%;box-shadow:0 20px 60px rgba(0,0,0,.3)}
  .modal-icon{width:48px;height:48px;border-radius:12px;background:#FEF2F2;display:flex;align-items:center;justify-content:center;margin-bottom:16px;color:#991B1B}
  .modal-title{font-size:18px;font-weight:800;color:var(--navy-dark);margin-bottom:8px}
  .modal-body{font-size:14px;color:var(--gray-700);line-height:1.6;margin-bottom:16px}
  .modal-actions{display:flex;gap:10px;justify-content:flex-end}
  .modal-divider{border:none;border-top:1px solid var(--gray-100);margin:12px 0}
  .btn{padding:0 28px;height:46px;border-radius:10px;font-family:'IBM Plex Sans Arabic',sans-serif;font-size:14px;font-weight:700;cursor:pointer;border:none;transition:all .2s;display:inline-flex;align-items:center;gap:8px}
  .toast{position:fixed;bottom:24px;left:50%;transform:translateX(-50%);padding:12px 28px;border-radius:12px;font-size:14px;font-weight:600;z-index:9999;box-shadow:0 8px 24px rgba(0,0,0,.15);opacity:0;transition:opacity .3s;pointer-events:none}
  .toast.show{opacity:1}
  .toast-success{background:#0F6E56;color:#fff}
  .toast-error{background:#991B1B;color:#fff}
  @@media(max-width:768px){.form-grid{grid-template-columns:1fr}.two-col{grid-template-columns:1fr!important}}
</style>

    <div class="page-header">
      <h2 data-i18n="updateStudentStatus">تحديث حالة الطالب</h2>
      <p data-i18n="updateStudentStatusDesc">تسجيل وتحديث الحالة الأكاديمية وبيانات السكن الجامعي للطالب</p>
    </div>

    <div class="form-card">
      <div class="form-card-header">
        <div class="form-card-header-icon">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#1B2A5E" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
        </div>
        <div class="form-card-title" data-i18n="registerNewStatus">تحديث الحالة الأكاديمية للطالب</div>
      </div>
      <div class="form-card-body">
        <div class="form-grid">
          <div class="field full">
            <label data-i18n="studentNumber">الرقم الجامعي <span style="color:#EF4444">*</span></label>
            <div class="input-wrap">
              <input type="text" id="studentNumber" data-i18n-placeholder="studentNumberPlaceholder" placeholder="مثال: 123456789" autocomplete="off">
            </div>
            <div class="field-error" id="studentNumberError" data-i18n="studentNumberError">الرجاء إدخال رقم جامعي صحيح</div>
          </div>

          <div id="studentInfo" style="display:none;grid-column:1/-1;padding:12px 16px;border-radius:10px;background:var(--gray-50)">
            <div style="font-size:13px;font-weight:600;color:var(--navy-dark)" id="studentInfoText"></div>
          </div>

          <div class="field full">
            <label data-i18n="statusType">نوع الحالة <span style="color:#EF4444">*</span></label>
            <div id="statusOptions">
              <div class="status-option" data-value="graduated" onclick="selectStatus(this)">
                <input type="radio" name="statusType" value="graduated">
                <div>
                  <div class="status-option-text" data-i18n="studentStatusGraduated">تخرج من الكلية</div>
                  <div class="status-option-desc" data-i18n="studentStatusGraduatedDesc">الطالب أكمل متطلبات التخرج بنجاح</div>
                </div>
              </div>
              <div class="status-option" data-value="dismissed" onclick="selectStatus(this)">
                <input type="radio" name="statusType" value="dismissed">
                <div>
                  <div class="status-option-text" data-i18n="studentStatusDismissed">فصل من الكلية</div>
                  <div class="status-option-desc" data-i18n="studentStatusDismissedDesc">صدر قرار فصل أكاديمي بحق الطالب</div>
                </div>
              </div>
              <div class="status-option" data-value="transferred" onclick="selectStatus(this)">
                <input type="radio" name="statusType" value="transferred">
                <div>
                  <div class="status-option-text" data-i18n="studentStatusTransferred">تحويل إلى جامعة أخرى</div>
                  <div class="status-option-desc" data-i18n="studentStatusTransferredDesc">تم تحويل الطالب إلى جامعة أخرى</div>
                </div>
              </div>
              <div class="status-option" data-value="left_housing" onclick="selectStatus(this)">
                <input type="radio" name="statusType" value="left_housing">
                <div>
                  <div class="status-option-text" data-i18n="studentStatusLeftHousing">غادر الإسكان الجامعي</div>
                  <div class="status-option-desc" data-i18n="studentStatusLeftHousingDesc">الطالب غادر السكن الجامعي نهائياً</div>
                </div>
              </div>
            </div>
            <div class="field-error" id="statusTypeError" data-i18n="statusTypeError">الرجاء اختيار نوع الحالة</div>
          </div>

          <div class="field full">
            <label data-i18n="actionReason">سبب الإجراء <span style="color:#EF4444">*</span></label>
            <div class="input-wrap">
              <textarea id="notes" data-i18n-placeholder="actionReasonPlaceholder" style="width:100%;height:100px;background:var(--gray-50);border:1.5px solid var(--gray-200);border-radius:10px;padding:12px;font-family:'IBM Plex Sans Arabic',sans-serif;font-size:14px;color:var(--navy-dark);direction:inherit;outline:none;resize:vertical;transition:all .2s" placeholder="أدخل سبب الإجراء (إلزامي، 5 أحرف على الأقل)"></textarea>
            </div>
            <div class="field-error" id="notesError" data-i18n="actionReasonError">الرجاء إدخال سبب الإجراء (5 أحرف على الأقل)</div>
          </div>

          <div class="field full">
            <label data-i18n="supportingDocument">إرفاق مستند داعم (اختياري)</label>
            <div class="file-upload-wrap">
              <input type="file" id="attachmentFile" accept=".pdf,.jpg,.jpeg,.png,.docx" onchange="updateFileName(this)">
              <div class="file-upload-btn">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="17 8 12 3 7 8"/><line x1="12" y1="3" x2="12" y2="15"/></svg>
                <span data-i18n="chooseFile">اختيار ملف</span>
              </div>
            </div>
            <div class="file-name" id="fileName"></div>
          </div>
        </div>

        <div style="margin-top:24px;display:flex;gap:12px">
          <button class="btn btn-primary" id="submitBtn" onclick="handleSubmit()" disabled>
            <span data-i18n="confirmAction">تأكيد الإجراء</span>
          </button>
          <button class="btn" style="background:var(--gray-100);color:var(--gray-700)" onclick="resetForm()">
            <span data-i18n="clear">مسح</span>
          </button>
        </div>
      </div>
    </div>

    <div class="card">
      <div class="card-header">
        <div class="card-title" data-i18n="recentActions">آخر الإجراءات</div>
      </div>
      <div class="history-list" id="historyList">
        <div style="text-align:center;padding:30px;color:var(--gray-500)" data-i18n="noData">لا توجد حالات</div>
      </div>
    </div>




<div class="modal-overlay" id="confirmDialog">
  <div class="modal-box">
    <div class="modal-icon">
      <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"/><line x1="12" y1="9" x2="12" y2="13"/><line x1="12" y1="17" x2="12.01" y2="17"/></svg>
    </div>
    <div class="modal-title" data-i18n="confirmDisableTitle">تحذير</div>
    <div class="modal-body" id="confirmBody">
      <p data-i18n="confirmDisableIntro">سيتم تعطيل حساب الطالب في شبكة الإسكان.</p>
      <hr class="modal-divider">
      <p><strong data-i18n="adUsernameLabel">اسم المستخدم:</strong> <span id="confirmUsername">--</span></p>
      <p><strong data-i18n="actionReasonLabel">سبب الإجراء:</strong> <span id="confirmStatus">--</span></p>
      <hr class="modal-divider">
      <p data-i18n="confirmDisableQuestion">هل تريد المتابعة؟</p>
    </div>
    <div class="modal-actions">
      <button class="btn" style="background:var(--gray-100);color:var(--gray-700)" onclick="closeConfirmDialog()" data-i18n="cancel">إلغاء</button>
      <button class="btn btn-primary" id="confirmYesBtn" onclick="executeSubmit()" style="background:#991B1B;color:#fff" data-i18n="confirmDisableYes">نعم، تعطيل الحساب</button>
    </div>
  </div>
</div>

<div class="toast" id="toast"></div>


@section Scripts {
<script src="/i18n.js"></script>
<script src="/js/supervisor-departure-page.js"></script>
}
